<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.tripinfo.repository.LocationBoardMapper">

    <!-- API 등록 -->
    <insert id="insertTable" parameterType="com.project.tripinfo.model.Table_name">
        INSERT INTO location_board (addr1, areacode, cat1, cat2, cat3, contentid, contenttypeid, createdtime,
                                    firstimage,firstimage2,mapx, mapy, mlevel, modifiedtime, title, zipcode, sigungucode)
        VALUES (
                #{addr1},#{areacode},#{cat1}, #{cat2}, #{cat3}, #{contentid}, #{contenttypeid},
                #{createdtime}, #{firstimage}, #{firstimage2}, #{mapx}, #{mapy}, #{mlevel}, #{modifiedtime}, #{title},
                #{zipcode}, #{sigungucode}
                )
    </insert>

    <!-- API 디테일 등록   -->
    <update id="insertDetailTable" parameterType="com.project.tripinfo.model.Table_name">
        UPDATE location_board
        SET overview = #{overview}
        WHERE contentid = #{contentid}
    </update>

    <!-- API 디테일 Content 체크   -->
    <select id="checkDetail" resultType="map">
        SELECT contentid
        FROM location_board
        WHERE overview IS NULL
    </select>

    <!--  API 테이블 중복 체크  -->
    <select id="checkDetailTable" resultType="int" parameterType="int">
        SELECT count(1)
        FROM location_board
        WHERE contentid = #{contentid}
    </select>

    <!-- API 디테일 중복 체크   -->
    <select id="checkTable" resultType="int" parameterType="int">
        SELECT count(1)
        FROM location_board
        WHERE contentid = #{contentid}
                AND contenttypeid = #{contenttypeid}
    </select>

    <!-- 홍대 페이지 조회  -->
    <select id="selectHongdae" resultType="hashmap" parameterType="hashmap">
        SELECT *
        FROM location_board
        WHERE areacode = 1
                AND sigungucode = 13
        <if test="contenttypeid != 0 and contenttypeid != null">
                AND contenttypeid = #{contenttypeid}
        </if>
        <if test='criteria.searchType.equals("board_title")'>
                AND title LIKE concat('%', #{criteria.keyword}, '%')
        </if>
        ORDER BY info_seq DESC
        LIMIT #{criteria.pageStart}, #{criteria.amount}
    </select>

    <!-- 홍대 상세 페이지 조회  -->
    <select id="hongdaeDetail" resultType="com.project.tripinfo.model.Table_name">
        SELECT *
        FROM location_board
        WHERE contentid = #{contentId}
                AND contenttypeid = #{typeId}
    </select>

    <!--  홍대 게시글 갯수 조회  -->
    <select id="hongdaeListCnt" resultType="java.lang.Integer" parameterType="hashmap">
        SELECT count(*)
        FROM location_board
        WHERE areacode = 1
                AND sigungucode = 13
        <if test="contenttypeid != 0 and contenttypeid != null">
                AND contenttypeid = #{contenttypeid}
        </if>
        <if test='criteria.searchType.equals("board_title")'>
                AND title LIKE concat('%', #{criteria.keyword}, '%')
        </if>
    </select>

    <!--  강남 페이지 조회  -->
    <select id="selectGangnam" resultType="hashmap" parameterType="hashmap">
        SELECT *
        FROM location_board
        WHERE areacode = 1
                AND sigungucode = 1
        <if test="contenttypeid != null and !contenttypeid.equals('')">
                AND contenttypeid = #{contenttypeid}
        </if>
        <if test='criteria.searchType.equals("board_title")'>
                AND title LIKE concat('%', #{criteria.keyword}, '%')
        </if>
        ORDER BY info_seq DESC
        LIMIT #{criteria.pageStart}, #{criteria.amount}
    </select>

    <!--  강남 상세 페이지 조회  -->
    <select id="gangnamDetail" resultType="com.project.tripinfo.model.Table_name">
        SELECT *
        FROM location_board
        WHERE contentid = #{contentId}
                AND contenttypeid = #{typeId}
    </select>

    <!-- 강남 게시글 갯수 조회  -->
    <select id="gangnamListCnt" resultType="java.lang.Integer" parameterType="java.lang.Integer">
        SELECT count(*)
        FROM location_board
        WHERE areacode = 1
                AND sigungucode = 1
        <if test="contenttypeid !=null and !contenttypeid.equals('')">
                AND contenttypeid = #{contenttypeid}
        </if>
    </select>

    <!--  명동 페이지 조회  -->
    <select id="selectMyeongdong" resultType="hashmap" parameterType="hashmap">
        SELECT *
        FROM location_board
        WHERE areacode = 1
                AND sigungucode = 24
        <if test="contenttypeid != null and !contenttypeid.equals('')">
                AND contenttypeid = #{contenttypeid}
        </if>
        <if test='criteria.searchType.equals("board_title")'>
                AND title LIKE concat('%', #{criteria.keyword}, '%')
        </if>
        ORDER BY info_seq DESC
        LIMIT #{criteria.pageStart}, #{criteria.amount}
    </select>

    <!--  명동 상세 페이지 조회  -->
    <select id="myeongdongDetail" resultType="com.project.tripinfo.model.Table_name">
        SELECT *
        FROM location_board
        WHERE contentid = #{contentId}
                AND contenttypeid = #{typeId}
    </select>

    <!-- 명동 게시글 갯수 조회  -->
    <select id="myeongdongListCnt" resultType="java.lang.Integer" parameterType="java.lang.Integer">
        SELECT count(*)
        FROM location_board
        WHERE areacode = 1
                AND sigungucode = 24
        <if test="contenttypeid !=null and !contenttypeid.equals('')">
                AND contenttypeid = #{contenttypeid}
        </if>
    </select>

    <!-- 잠실 페이지 조회  -->
    <select id="selectJamsil" resultType="hashmap" parameterType="hashmap">
        SELECT *
        FROM location_board
        WHERE areacode = 1
                AND sigungucode = 18
        <if test="contenttypeid != null and !contenttypeid.equals('')">
                AND contenttypeid = #{contenttypeid}
        </if>
        <if test='criteria.searchType.equals("board_title")'>
                AND title LIKE concat('%', #{criteria.keyword}, '%')
        </if>
        ORDER BY info_seq DESC
        LIMIT #{criteria.pageStart}, #{criteria.amount}
    </select>

    <!-- 잠실 상세 페이지 조회  -->
    <select id="jamsilDetail" resultType="com.project.tripinfo.model.Table_name">
        SELECT *
        FROM location_board
        WHERE contentid = #{contentId}
                AND contenttypeid = #{typeId}
    </select>

    <!-- 잠실 게시글 갯수 조회  -->
    <select id="jamsilListCnt" resultType="java.lang.Integer" parameterType="java.lang.Integer">
        SELECT count(*)
        FROM location_board
        WHERE areacode = 1
                AND sigungucode = 18
        <if test="contenttypeid !=null and !contenttypeid.equals('')">
                AND contenttypeid = #{contenttypeid}
        </if>
    </select>

    <!-- 이태원 페이지 조회  -->
    <select id="selectItaewon" resultType="hashmap" parameterType="hashmap">
        SELECT *
        FROM location_board
        WHERE areacode = 1
                AND sigungucode = 21
        <if test="contenttypeid != null and !contenttypeid.equals('')">
                AND contenttypeid = #{contenttypeid}
        </if>
        <if test='criteria.searchType.equals("board_title")'>
                AND title LIKE concat('%', #{criteria.keyword}, '%')
        </if>
        ORDER BY info_seq DESC
        LIMIT #{criteria.pageStart}, #{criteria.amount}
    </select>

    <!-- 이태원 상세 페이지 조회  -->
    <select id="itaewonDetail" resultType="com.project.tripinfo.model.Table_name">
        SELECT *
        FROM location_board
        WHERE contentid = #{contentId}
                AND contenttypeid = #{typeId}
    </select>

    <!-- 이태원 게시글 갯수 조회  -->
    <select id="itaewonListCnt" resultType="java.lang.Integer" parameterType="java.lang.Integer">
        SELECT count(*)
        FROM location_board
        WHERE areacode = 1
                AND sigungucode = 21
        <if test="contenttypeid !=null and !contenttypeid.equals('')">
                AND contenttypeid = #{contenttypeid}
        </if>
    </select>

    <!-- 이태원 페이지 조회  -->
    <select id="selectGwanghwamun" resultType="hashmap" parameterType="hashmap">
        SELECT *
        FROM location_board
        WHERE areacode = 1
                AND sigungucode = 23
        <if test="contenttypeid != null and !contenttypeid.equals('')">
                AND contenttypeid = #{contenttypeid}
        </if>
        <if test='criteria.searchType.equals("board_title")'>
                AND title LIKE concat('%', #{criteria.keyword}, '%')
        </if>
        ORDER BY info_seq DESC
        LIMIT #{criteria.pageStart}, #{criteria.amount}
    </select>

    <!-- 이태원 상세 페이지 조회  -->
    <select id="gwanghwamunDetail" resultType="com.project.tripinfo.model.Table_name">
        SELECT *
        FROM location_board
        WHERE contentid = #{contentId}
                AND contenttypeid = #{typeId}
    </select>

    <!-- 이태원 게시글 갯수 조회  -->
    <select id="gwanghwamunListCnt" resultType="java.lang.Integer" parameterType="java.lang.Integer">
        SELECT count(*)
        FROM location_board
        WHERE areacode = 1
                AND sigungucode = 23
        <if test="contenttypeid !=null and !contenttypeid.equals('')">
                AND contenttypeid = #{contenttypeid}
        </if>
    </select>
</mapper>